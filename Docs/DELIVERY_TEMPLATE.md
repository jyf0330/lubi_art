# 美术组件交付记录模板

复制本模板到任务报告或交付说明中填写。`N/A` 必须附原因，不能用空白代表通过。

## 1. 交付对象

```text
组件/页面名称：
变更类型：图片 / 布局 / 交互 / 动效 / 接口 / 完整页面
场景入口：res://
公开脚本：res://
目标程序路径：
负责人：
日期：
```

## 2. 依赖与公开契约

```text
依赖资源：
Shared 依赖：
公开方法：
公开信号：
ViewModel 必需字段：
空值与极值处理：
```

## 3. 表现与适配

```text
基准 1920×1080：PASS / FAIL / N/A（原因）
宽屏 2560×1080：PASS / FAIL / N/A（原因）
窄屏 1440×1080：PASS / FAIL / N/A（原因）
声明的最小尺寸：
长文本/大数值：PASS / FAIL / N/A（原因）
default/hover/pressed/disabled：PASS / FAIL / N/A（原因）
拖拽/合法落点/取消恢复：PASS / FAIL / N/A（原因）
```

## 4. 动画与性能

```text
触发时点：
命中/落位时点：
结束信号：
重复/连播/中断清理：PASS / FAIL / N/A（原因）
10 次页面切换或特效重播：PASS / FAIL / N/A（原因）
修改前平均帧时间：
修改后平均帧时间：
节点或孤立实例持续增长：无 / 有 / N/A（原因）
```

## 5. 验证证据

```text
执行的 smoke test：
测试结果：
真实 Godot 入口：
截图路径与说明：
录屏路径、关键时间点与说明：
Godot 新增错误：无 / 有（列出）
```

## 6. 迁移边界与例外

```text
保留的节点结构/几何/动画：
由正式程序替换的 Mock/Controller/规则：
未接入部分：
规则例外：无 / 规则 + 原因 + 影响范围 + 负责人 + 移除条件
已知限制：
```

## 7. 最终结论

```text
结论：PASS / FAIL / BLOCKED
阻塞项：
下一步：
```
